using UnityEngine;
using UnityEngine.UI;

public class IngredientHeatSliderScript : MonoBehaviour
{
    private Slider slider;
    void Start()
    {
        slider = GetComponentInChildren<Slider>();
    }

    void Update()
    {
        
    }
}
